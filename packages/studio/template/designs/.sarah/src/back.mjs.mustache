import { back as sarahBack } from '@freesewing/sarah'

export const back = {
  from: sarahBack,
  name: '{{ name }}.back',
  hide: { from: true },
  measurements: [],
  options: {},
  draft: ({ part }) => {
    return part
  },
}
